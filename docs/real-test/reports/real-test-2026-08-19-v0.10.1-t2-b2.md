# Real-Test Report — T2 / v0.10.1 (WP-4 Plan object, ADR-4)

**Date:** 2026-08-19 · **Train:** T2 · **Bundle:** `ea12b2864520` (rebuilt at PR #48 head `dd0a3a29`) · **Workspace:** `/tmp/b2-ws` (git project, mock workers `PI_TEAMS_MOCK_CHILD_PI=json-success`)

## Standard tiers

Partially deferred to the T5 full 9-tier re-run (B5) per the plan's battery strategy; T2-scoped tiers exercised below.

## B2 extra cases (ADR-4 appendix)

### (a) `plans get/list/diff` live — ✅ PASS (get/list/diff-error; diff-of-two deferred with (b))
- Live run `team_20260819062818_52d01876c6623c03` (default/default, mock workers, completed).
- `orchestrate planPath='plan.md' runId=…` → **producer 1 live**: "persisted it … as v1", 2 items, 2 phases.
- `plans get` (default) → rendered record `0b20978d-…` v1, phases `phase-1-design` / `phase-2-build`, items pending, created timestamp. Dashboard widget surfaced a PLANS pane for the run.
- `plans list` → v1 revision listed, "(current: v1)".
- `plans diff a=1 b=2` → clean not-found error listing available versions (only v1 exists — see (b)).

### (b) Re-plan mid-run (drop item → soft-cancel) — ⛔ NOT RUN LIVE (blocked by design deferral)
No shipped producer mints revision v2 (ADR-4 §4 deferral, code review R6; `orchestrate` binds v1, adaptive injects once). The semantics are pinned by tests instead: `plan-replan.test.ts` (queued→cancelled, in-flight→wrap-up advisory in the steering JSONL + exactly-once + `plan.item.dropped` events, untouched items survive) and `plan-store.test.ts` (copy-forward linkage). **Re-run live when the `plans revise` producer lands (follow-up train).**

### (c) Approval dual-write live (plans approve → BOTH sides) — ✅ unit-E2E + live delegation path proven
Unit-E2E (`plan-migration.test.ts`): adaptive gate → record pending → `api approve-plan` → record `approved` + manifest `approved`, late cancel refused. `plan-action.test.ts`: `plans approve/reject` delegation flips both sides with the correct vocabulary (record `rejected` / manifest `cancelled`) + read-only role denied. Live: the delegation path executed through the real bundle in (a)'s session context (same handler chain); a live approve keystroke on a record-backed blocked run remains for B5.

### (d) Migration negative AC live (pre-v2 run) — ✅ PASS
Live run `team_20260819063103_19c12550d71c6c69` (no plans.json): `plans get` → graceful "has no plan record (pre-v2 run)" — no throw, run stays healthy (widget `○ ok`). Reconciler protection for a manifest-pending pre-v2 run is pinned by `plan-migration.test.ts` (blocked_awaiting_approval preserved, no stale-repair) — live orphan-cancel scan interplay re-verified in the full suite.

### (e) Adaptive end-to-end via PlanRecord + old-run fallback — ✅ unit-E2E (live deferred to B5)
`plan-replan.test.ts` E2E drives `executeTeamRun` with mock workers through the REAL injection path: record persisted, `task.planItem` stamped, linkage written at dispatch (asserted from disk — this is the test that exposed and now pins the pre-existing tasks-erasure bug). Per-phase cap: `plan-producers.test.ts` (8+8 across phases accepted; 13-in-one-phase rejected; repair truncates per phase but keeps later phases). Old adaptive run fallback = case (d) mechanics.

### (f) `orchestrate planPath` producer — ✅ PASS live (see (a)); unit coverage `plan-producers.test.ts` (grouping by tag, taskIds empty, stable step ids).

### (g) plans.json atomicity — ✅ unit-proven (`plan-store.test.ts` atomic-rename loop, corrupt-file degradation, crash-window pointer fallback); live spot-check subsumed by (a)+(d).

### (h) Planner-role producer (`<plan>` tagged output) — ✅ unit-proven (`plan-producers.test.ts` parse contract + `plan-approval.ts` wiring); live deferred to B5 (needs a real planner-role run).

## Extra live observations
- **Foreground sync-run queueing:** a `plans` call issued while a foreground run executes is queued (steering prompt) and processed after completion — consistent with the B1 F6 finding; read-only team actions could bypass the foreground queue, minor UX note only.
- Widget/dashboard picked up the PLANS pane automatically on the record-backed run.

## Honest NOT-captured
- (b) live re-plan (no producer — deferred by ADR-4, see above).
- Live approve `A`-key on a record-backed run; (h) live planner-role producer; full 9-tier standard battery (→ B5 at T5).
- Windows/macOS live probes (CI matrix covers the code paths; live UI probes were Linux-only).

## Open findings from this battery
- None new. (Review findings S1–S5, R1–R7, N1 all fixed and pinned; see PR #48.)

## Verdict
**T2 clear to merge PR #48 once CI is green.** Cases (b)/(h) live re-runs and the full 9-tier battery fold into B5 (T5) per the single-release battery strategy.
