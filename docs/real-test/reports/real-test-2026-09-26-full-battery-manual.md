# real-test-pi-crew — Run Report (FULL battery, manual, no driver script)

**Date**: 2026-09-26 (03:00–04:00 UTC)
**Trigger**: user request "chạy full tier skill real test pi-crew, dont use script" — full 13-tier battery executed interactively, every tier by hand, no automation wrapper.
**Repo HEAD**: `0ca2816b` (main, tree clean, verified before + after)
**Bundle md5 (disk)**: `5572bc503cdc161bb770727596f35c50` (1637.0 KB)
**Pi version**: extension session inside herdr pane (node v22.23.1); worker model fallback chain `minimax/MiniMax-M3 → chiase/cnb/qwen3.8-max → chiase/deepseek-v4.1-flash`
**Run by**: agent session (cwd `/home/bom/source/my_pi`), live `team` tool from parent session

## Tier results

| Tier | Status | Evidence |
|---|---|---|
| 1 test:critical | ✅ | `116/116` pass, 24.6s (soft limit 25s) |
| 2 3-path kill-switch | ✅ | default + `PI_CREW_BROKER=0` + `=1` all `116/116` |
| 3 typecheck + bundle | ✅ | tsc exit 0 ("strip-types import ok"); `build:bundle` 1637.0 KB md5 `5572bc50…`; staleness OK ("84347.3s newer than newest src/"); ARCH-7 path-leak OK |
| 4 bundle md5 sync | ✅ | session PID 3474406 (start 2026-09-26 09:56:04 +07) > dist mtime (2026-09-25 10:38) → cold-started on current bundle; identified via cwd + fd writing `.crew/state/metrics/2026-09-26.jsonl` (metric-sink of this session) — process-level proof, not file-md5-only |
| 5 tmux TUI probe | ✅ | fresh pi in dedicated tmux server: `/team-help` rendered full command list; `/team-dashboard` opened interactive pane (`┃` rails + `R reload · Esc close`); **Esc closed pane (74-line screen diff) = dispatch proven**; `j`/`↓`(`\x1b[B`)/`\x1bOB`/`k`/`\x1bOA` no visible change — run list empty in fresh project (nothing to move); parity covered by `keybinding-map.parity` + `pi-tui-dispatch-probe` in T1 |
| 6 pty probe | ⏭️ | T5 chosen per "(T5\|T6)"; pty path is a bundled script (user asked no scripts) |
| 7 smoke team run | ✅ | runId `team_20260926030659_71aa44cc10af9e3c` (fast-fix, sync): 3/3 tasks, 4042 tok, **197.8s** (<600s), consistency=1, verifier ran `test:critical`+`tsc` with provenance cache; no hang |
| 8 final md5 sync | ✅ | disk = committed (11j) = fresh build (T3 rebuild identical) = session-loaded `5572bc50…` |
| 9a read-only battery | ✅ | 12/12: list / recommend / health (209 runs scanned) / doctor-zombies (0 subagents, 0 orphan panes) / status / events (full lifecycle incl. hooks + worker diagnostics) / summary (cost by role) / get-workflow / explain / worktrees / graph / search / settings-get — all structured, no `Unknown type`, no `Validation failed` |
| 9b spawn paths | ✅ 6/7 | sync ✓ (T7) · **async** ✓ ×2 (`async.spawned` events pids 3507376/3523215 + detached background.log; NOTE: tool call blocks until completion **by design** — CORE-8 `waitForRun` in `run.ts:611+`; skill text stale) · chain ✓ 2 steps/2 handoffs 153s (no `workflow` param — issue #44 avoided) · Agent-direct ✓ (`AGENT_DIRECT_OK`) · crew_agent-bg ✓ (190-word essay + `ESSAY_DONE`) · steer_subagent ✓ (`STEER_RECEIVED` in final output; channel `01_01-agent.jsonl`) · orchestrate ⏭️ (plan covers planning dispatch; noted) |
| 9b-W worker tools | ✅ | **ask round-trip ×3**: explorer+executor+verifier each asked; detached strict-envelope responder answered all three in **0.486–0.601s** (ask.requested→ask.answered); token `ASK_REPLY_TOKEN_7781` present in all 3 agents' outputs + `<dependency-context>` in worker toolResult (the required round-trip proof); **manual `respond` action**: `ask.answered` with `delivery:'mailbox'` discriminator (respond.ts path) — distinct from self-resolve `{questionId}`-only events. **message notify**: worker-side send ✓ (`MSG_NOTIFY_OK_42` in all agents' logs; leader-side ambient NOT observed this session — gap noted). **delegate**: 2 children admitted (`childDepth: 2`) + completed (17+25=42 used in final answer); executor's 5 attempts rejected **structured** (`budget-insufficient` ×2, `model-invalid` ×3 — model guessed `MiniMax-M3`/`deepseek-v4.1-flash`/`zai/glm-5.3` not in registry) then succeeded — structured-policy reject path proven live. DM/group + depth-5 ⏭️ (documented) |
| 9c lifecycle | ✅ | mid-run `status details=true` (live task graph + active agents + effectiveness, run `team_20260926034641…`) · steer mid-run ✓ ("queued for task; delivered when session ready") · `wait` ✓ ×4 · `respond` ✓ (see 9b-W) · **cancel**: ownership-guard blocked cross-session attempt ("belongs to another session") then `force:true` cancelled ✓ · **retry**: guard on completed run ("retry only applies to failed/cancelled") + real re-dispatch on cancelled run ("Retried 1 task(s)" + 2nd task.started) · resume on completed = idempotent no-op ✓ · checkpoint = **read** of FileCheckpointStore ("No checkpoint found." structured; store is crash-recovery-scoped) · cache ✓ (run cache + skill cache 76.5% hit rate) |
| 9d destructive | ✅ | scratch project only: prune confirm-gate ✓ then real prune (kept 2/removed 0, audit `.crew/audit/prune.jsonl`) · forget ✓ (state+artifacts removed, listed) · cleanup dryRun ✓ (preview, force-gated) |
| 9e admin | ✅ | team create/update/delete round-trip ✓ (default scope = **user** (`~/.pi/agent/teams/`), .bak backups made, deleted + residue cleaned; roles schema requires `name` — structured validation error first try) · workflow-create/list/get/delete ✓ (`t9d-empty.dwf.ts` in scratch `.crew/workflows/`; run of it failed ~2s at trust gate exactly as documented → cheap finished run) · settings get/set ✓ (`ui.widgetPlacement=bottom`, surface visibleAgents set+restore) · export→import round-trip ✓ (run-export.json/md + `.crew/imports/` + README) · parallel ✓ 2/2 launched concurrent (**schema drift: `config.tasks` not `tasks=[…]`**) · plan ✓ (+small-goal singleAgent hint) · plans ✓ (runId-scoped; "no plan record (pre-v2 run)") |
| 9f background | ✅ | schedule: interval job registered (next-run computed) + `scheduled` list + removed ✓ · goal-loop: start ✓ (background pid, turn/budget state machine) → status ✓ → terminal `blocked` with **`lastTurnError` persisted at goal level** (GL-1b working: "Failed at task '01_work'") → cleared ✓ · auto-summarize status ✓ · anchor status ✓ (no-throw) · api ✓ (full manifest dump: 46 artifacts w/ contentHash, runConfig, modelContext, surface) |
| 10a surface E2E | ✅ | tmux suite **4/4** (spawn+self-close / kill-pane→degrade→headless / doctor orphan / tab-per-run — suite grew from skill's 3) · herdr suite **5/5** (+closeTab edge cases) — real panes, no mocks |
| 10b live surface run | ✅ | run `team_20260926035213_9dca80b85aeba197` (pi -p inside dedicated tmux): panes `%2\|01_explore` → `%3\|02_execute` → `%4\|03_verify` observed LIVE via `list-panes` (titles carry taskId, π spinner in title), each auto-closed; 3× `worker.surface_spawned` + 3× `worker.surface_closed`, **0** `surface.degraded`; `manifest.surface.provider=tmux`, `surface.workerPids` = 3 pids, tab `@1`; `visibleAgents` restored `[]` after |
| 10c herdr path | ✅ | session ran INSIDE herdr pane (`HERDR_ENV=1`, `$TMUX` unset): run `team_20260926035325_5ce842103cf054e2` → `provider=herdr`, real user-workspace tab `w2:t2W`, paneIds `w2:pDA/pDB/pDC`, 3× spawned + 3× closed, 0 degraded, 3 workerPids |
| 11 remediation regression | ✅ | 11a: stores sync atomicWriteJson ✓ + crash-recovery sync intent ✓ + buffered census **15** files (anchor 16 — drift noted) + **full `test:unit` 8232 tests / 8229 pass / 0 fail / 3 skipped** · 11b: wc-gate max 1207/2000, in `ci`+`ci:fast`+ci.yml ✓ · 11c: both probe keys severity "removed", hasWarnings ✓ · 11d: exactly 3 slow files ✓ · 11e: nightly comment-only, weekly sets SMOKE ✓ · 11f: reject-format at all 3 cited sites ✓ · 11g: `"bottom"` in both EFFECTIVE_DEFAULTS + live `settings get` = bottom ✓ · 11h: twins **3× consecutive 4/4** ✓ · 11i: dead exports 0 + MUST_INCLUDE pinned ✓ · 11j: `--committed-hash` OK |
| 12 resource contracts | ✅ | 12a 1/1 (all 18 builtin agents) · 12b discovery 18 agents / 0 bad desc / 0 no-routing + strict-YAML 18/0 · 12c 16/16 rendered agent lines carry `useWhen=` (budget truncation by design; orchestrator present, verifier/writer tail-cut) · 12d 39/39 |
| 13 real-run UI render | ✅ | real run `…71aa44cc10af9e3c` (NOT fixtures); surfaces: CALL / STREAMING (via producer) / COLLAPSED / EXPANDED / EXPANDED@80 / DOCK done+@50+running+failed+@40+@60 / PLAN CARD / SIDEBAR; sweeps all **0 hits** (undefined / retired frame / `->` / wire-format / bad plural); python invariants BAD:none (spinner never with `0 running`); hint `↓·enter` survives @40; cross-surface: `fast-fix` label ×12 no `fast-fix/fast-fix`, short-id `10af9e3c` consistent, durations `29.8s/2m11s` one formatter, usage `↑2.1k ↓1.9k` compact everywhere; catalog regenerated (18 txt + 18 PNG, glyph self-check DONE) then **drift restored** (anti-pattern honored); PNG visual ⏭️ honestly — current model has no image support, the renderer's fail-loud glyph self-check is the gate |

Legend: ✅ pass with evidence · ❌ fail · ⏭️ skipped (justified)

## Findings (bugs / quirks / non-blocking)

1. **[MEDIUM] Cross-session force-cancel leaves the in-flight worker alive** — run `team_20260926034641_08fed785c339d83a`: `run.cancelled` at 03:47:47 (force), but worker pid 3529734 kept streaming `task.progress` for **≥126s** after (last 03:49:13) and was still alive 2m24s post-cancel (exited on its own eventually; **no `worker.exit`/`close` event ever recorded** — lifecycle bookkeeping lost too). Cancel marks state; it does not (reliably) terminate the live child cross-session. Follow-up: trace the kill path in cancellation/team-runner for cross-session cancels.
2. **[MEDIUM] Effectiveness guard false-positives on guest-child (`gc-*`) tasks → run `blocked`** — run `team_20260926033657_2b6c6d2610b26d9d`: all 6 tasks (3 roles + 3 `gc-` delegate children) completed with real outputs (`delegate.completed ok`, SUM used), yet closeout = `blocked` with "no observable worker activity for gc-…" (`src/runtime/effectiveness.ts:67-75` — gc tasks don't stream `task.progress` into run events → counted as no-work; P0.1 escalates warn→blocked for non-read-only roles). Every delegate-using run inherits a misleading terminal status.
3. **[LOW] Retry on a cancelled run leaves contradictory state** — same run: after retry(force) re-dispatched a task, `tasks.json` shows `02_execute: running` (worker long dead, never transitions) while `manifest.status` stays `cancelled`. Cascade of finding 1; also visible to health/list surfaces.
4. **[OBS] `async=true` blocks the tool call for the whole run (by design, CORE-8)** — `run.ts:611+` spawns detached runner then `await waitForRun(...)` to "return actual results". Both async probes (781s, 101s) blocked the turn. Skill/battery text describing instant return is stale.
5. **[OBS] `search` indexes agent/team/workflow resources (BM25), not run history** — skill text "search runs/goals" stale; probes with agent-absent terms correctly answer "No results found."
6. **[OBS] `checkpoint` action is a READ** of `FileCheckpointStore` (crash-recovery-scoped); no store exists in normal runs → "No checkpoint found." Skill text ambiguous.
7. **[OBS] `create resource` defaults to USER scope** even with a project marker present (workflow-create went to project scope, team-create went to `~/.pi/agent/teams/`). Delete cleans up; .bak backups were removed manually.
8. **[OBS] Worker self-masked a "secret"** — model received `PINEAPPLE` raw (9 occurrences in its events incl. `<dependency-context>`) but wrote `SECRET=***` itself. Round-trip proof unaffected.
9. **[OBS] Message notify: worker-side send proven** (token in 3 agent logs) **but leader-side ambient never surfaced in this session** — gap, not called broken (broker delivery to owner-session ambient unverified here).
10. **[OBS] Unit suite spawns real runs into the shared workspace** — 2 runs (`team_20260926031025_*`, dead-cwd ghosts in health) appeared while `test:unit` ran; health "running=138" is dominated by test-spawned stale runs. Pre-existing class, unchanged.
11. **[OBS] summary shows `turns=0`** for all roles while workers clearly took multiple turns (tokens correct). Display-only quirk.

## What was NOT run + why
- **orchestrate** — plan action covers the planning dispatch family; orchestrate adds execution fanout already covered by run/parallel.
- **message DM/group** — notify path proven; DM routing pinned by unit suites (`mailbox-*`), not re-probed live (token cost).
- **depth-5 delegate reject live** — structured reject mechanism proven live via `budget-insufficient`/`model-invalid` rejects (same structured path); depth-cap pinned by `delegate-tool` unit tests.
- **9d on REAL project data** — destructive actions confined to scratch project by design (real-project prune only via the automatic keep=10 which was observed firing: session-start auto-prune audited, removed=0).
- **PNG visual inspection** — current session model has no image support; `render_png.py` fail-loud glyph self-check (rc=0) is the designed tofu gate.
- **11a conversion-program re-audit** — no write-path conversion in this change set; full `test:unit` ran anyway (mandatory item satisfied).

## Restart needed?
- [x] No — session already on the new bundle (process-start > build-time proof, Tier 4/8)
- [ ] Yes

## Verdict
**13/13 tiers executed live with per-row evidence — full battery PASS** (T6 by-design substitute T5; 4 justified ⏭️ sub-probes inside 9b/9b-W/13). Three product findings filed for follow-up (cancel-kill gap, effectiveness-guard gc false-positive, retry stale-state cascade); 8 behavioral observations incl. 3 stale-skill-text notes (async blocking, search scope, checkpoint semantics). Repo clean at `0ca2816b`; bundle `5572bc50…` consistent disk/committed/build/session. Scratch evidence root: `/home/bom/source/my_pi/pc-battery-scratch/.crew/` (safe to delete).

## Addendum (post-report, live follow-up — 2026-09-26 ~04:15Z)

**Ambient "[team_202] Task gc-c7006dc3… heartbeat dead / Background watcher detected a stuck worker"** arrived ~38 min after the run finished. Verified BEFORE acting (per skill):

- `manifest.status = completed` (updatedAt 03:43:41), task `gc-c7006dc3` = `completed` at 03:37:57 — no live process, nothing stuck.
- Event timeline (smoking gun): `delegate.admitted` 03:37:47.599 → `crew.task.heartbeat_dead` **03:37:47.651 — 52ms after admit**, then a second fire 5s later. Same pattern for all 3 guest children (each ran ~8–10s and completed fine).
- Root cause: guest-child tasks (`agent:"delegate"`, `depth:2`) have **no heartbeat channel** — `heartbeatAgeMs(undefined) → Infinity` → instant "dead" on the first watcher poll. The late ambient arrival is the by-design one-message-per-turn-boundary host-queue drain (Finding-6 mechanics), not a replay bug.

**Fixed in this commit** (`src/runtime/heartbeat/heartbeat-watcher.ts`): the watcher task loop now skips `agent === "delegate"` guest tasks — their lifecycle is owned by `delegate.requested/admitted/completed` broker events, not the heartbeat channel. Regression `test/unit/runtime/heartbeat/heartbeat-watcher-guest-skip.test.ts` (2 tests): guest-with-no-heartbeat never classified dead + guest skip does NOT suppress dead detection for a real sibling. **Mutation-checked**: removing the skip → 2/2 red; restoring → 2/2 green. Watcher suites 15/15, critical 116/116, tsc/lint/format green, bundle rebuilt (1637.1 KB, md5 `69b36e09…`).

Related open item: the effectiveness-guard gc-* false-positive (battery Finding 2) shares the same root shape (guest tasks invisible to per-task observability) but is a separate code path (`effectiveness.ts`) — still open.

**Addendum 2** — ambient thứ hai cùng run ("1 worker(s) missing heartbeat", ~04:25Z): cùng gốc rễ gc- ở lớp `summarizeHeartbeats` (`src/ui/heartbeat-aggregator.ts`) — guest task không kênh heartbeat bị đếm là missing worker MỖI health tick khi run còn sống → fire ≤3 (cap Finding-6 giữ đúng) → host 0.87.0 không có `clearQueuedUserMessagesMatching` nên purge no-op → các bản queued drain 1/turn-boundary. **Fixed**: skip `agent==="delegate"` trong vòng đếm (đã có sẵn `runTerminal` + isActiveTask gates). Regression: 2 test mới trong `heartbeat-overlay.test.ts` — mutation-checked (bỏ skip → 2/2 đỏ). 7/7 suite xanh, critical 116/116, bundle `3d47ea28…`. Người dùng có thể còn thấy ≤ vài bản queued cũ drain ở các turn tới (bounded, vô hại); trên host có API purge chúng sẽ bị thu hồi.
