# Real-Test B4 — Spec System (T4/R6, ADR-6) — v0.10.1 pre-merge

- **Date:** 2026-08-20
- **Branch/HEAD:** `v2/r6-spec` @ `a1d46866` (PR #50; ADR-6 + erratum §11)
- **Battery:** B4 per ADR-6 §"Gates" — spec-weighted
- **Reviewer model:** deepseek-v4-flash (security + code + verifier), per review policy

## Method

Live probes drive the **public production surfaces** in real git workspaces
(`buildTaskPacket` → `computeSpecGate`; `scripts/spec-import.mjs`; `handleTeamTool`
extension entry; no internal monkey-patching), backed by the unit matrix
(6956 unit + 188 integration green at review HEAD `45e8aeb9`; 30 sandbox tests
executable on userns-capable Linux).

## Case results

| Case | Result | Evidence |
| --- | --- | --- |
| **(a)** non-strict happy path | ✅ live | musts covered by footer → pass, **no badge** (`b4-probe.mts` a) |
| **(b)** non-strict fabrication (full coverage) | ✅ live | plausible-but-wrong evidence citing every id → passes **with NO badge** — corrected §3 honesty semantics (b) |
| **(c)** footer parser matrix per-mode | ✅ live + unit 17 | missing footer w/ musts → badge (non-strict) / **fail** (strict); unknown id → badge / **fail**; fabricated-but-known → covered / machine-check decides (unit: trailing-region, multi-block union, quoted-marker immunity) |
| **(d)** strict + manual minted spec | ✅ live | digest match → pass; digest mismatch → gate fails; exit-code-only mode works (probe d via `spec-import.mjs` mint) |
| **(e)** negative ACs ×2 | ✅ live | worker-written record (declared manual+trusted) degrades to coverage-only + badge, never re-run; **forged workspace `*.json`+`*.trusted` pair ALSO degrades** — workspace store structurally never mints trust (probe e) |
| **(f)** sandbox env containment | ✅ unit 2 | captured-env: no provider keys, no `PI_CREW_BROKER_*`; result key-set = scrubbed selection exactly (no-resurrection pinned) |
| **(g)** sandbox limits + netns | ✅ unit 30 + CI evidence | timeout group-kill (SIGTERM→SIGKILL); netns isolation (`/dev/tcp` probe); wrapper-launch failure **fails closed** — confirmed LIVE on GitHub ubuntu-24.04 runners (AppArmor `apparmor_restrict_unprivileged_userns=1`: every strict check failed closed; positive-execution tests now skip with B4-g note there); survivor group-kill on exit; `output-capped` distinct outcome |
| **(h)** non-idempotent strict must | ✅ live | coverage-only + `unverified`, recorded per-acceptance (probe h) |
| **(i)** reject-start | ✅ live ×2 | strict w/o verifier → hard error naming ADR-6 §5; strict + verifier + unresolvable specRef → hard error naming the ghost id (`handleTeamTool` scaffold runs) |
| **(j)** spec-less regression | ✅ suites | 6956 unit + 188 integration green; spec-less packets byte-identical (no specGate field/events/prompt block) — unit guards |
| **(k)** freeze immutability | ✅ live | snapshot unchanged after post-freeze spec edit; digest-swap on the minted record degrades re-freeze trust to false while the frozen snapshot keeps its bit (probe k) |
| **(l)** verifier advisory | ✅ unit | verifier prompt variant: "ADVISORY ONLY — the mechanical coverage gate and the strict machine-check decide, not you"; footer arrives via dependency-context (no new delivery surface) |

## Provenance v2 (round-1 P1 fix) — end-to-end

- Mint CLI `spec-import.mjs`: validates shape (incl. 64-hex digests), forces
  `source=manual+user`, writes ONLY the USER store
  (`~/.pi/agent/specs/<sha256-slug>/`) with digest-bound sidecar — smoke-tested
  clean (valid import, invalid-digest rejection exit 2).
- Attack surface re-check live: (1) worker path writes workspace store →
  forced generated, freeze untrusted; (2) forged workspace sidecar pair →
  mints nothing; (3) content-swap on minted record → digest mismatch →
  untrusted; (4) TOCTOU post-freeze sidecar delete → frozen snapshot
  unaffected. All four verified in probe (e)/(k).

## Review loop record

- Round 1: security (2×P1 + 2×P2 + 5×P3) + code (4×P2 + 7×P3) — **all applied**
  @ `03e22729` (provenance v2; tri-state specStrict; fail-closed freeze ×3
  layers; sandbox cwd; scaffold/already-failed skips; error prefixing; footer
  union incl. rawFinalText; survivor kill on exit; output-capped; event
  registry; coalesce exclusion; reject-start step-level + DWF).
- Round 2 (verifier, code-read): **ALL-CLOSED 16/16** + 3 residues → applied
  @ `45e8aeb9` (env-scrub result map — a lost batch-edit caught by reading
  code rather than the commit message; tri-state test pin; digest-format
  import validation).

## Honest NOT-captured

- The **executor SPEC contract inside a REAL model-driven worker TUI session**
  (footer compliance by a live LLM) — mock-worker + unit surfaces pin the
  mechanical contract; folded into B5's full 9-tier re-run.
- **macOS strict machine-checks** fail closed by design (no `unshare`); the
  loud platform warning path is unit-pinned only (no macOS strict run here).

## Open findings from this battery

- None new. (B4-g CI note resolved: userns-restricted runners fail closed —
  verified live on ubuntu-24.04; skip-with-note added for positive tests.)

## Verdict

**T4 clear to merge PR #50 once CI is green.** The spec system's security
posture is verified at every layer live-probeable today: provenance v2 trust
anchor (user store + digest sidecar + freeze-time bit), fail-closed freeze,
strict machine-check sandbox containment, and the non-strict coverage-only
default with honest badge semantics.
