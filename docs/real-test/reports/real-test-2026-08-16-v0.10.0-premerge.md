# real-test-pi-crew — Run Report

**Date**: 2026-08-16
**Trigger**: pre-merge PR #46 (v0.10.0) — user-required full 9-tier sweep before merge
**Repo HEAD**: `f6e605ef`
**Bundle md5 (disk)**: `f3b0e7e5b57290d3e208fa6fc39fb8f3` (2956.9 KB)
**Pi version**: 0.84.2
**Run by**: pi agent (glm-5.3), live session started 16:46:33 (+0700)

## Tier results

| Tier | Status | Evidence |
|---|---|---|
| 1 test:critical | ✅ | 101/101 pass, 14.1s |
| 2 3-path kill-switch | ✅ | default + `PI_CREW_BROKER=0` + `=1` all 101/101 (exit 0 ×3) |
| 3 typecheck + bundle | ✅ | tsc exit 0; bundle 2956.9 KB, md5 `f3b0e7e5…` — rebuild reproduced the committed bundle's md5 byte-identical (deterministic) |
| 4 bundle md5 sync | ✅ | disk = session-loaded = `f3b0e7e5…`; session cold-start 16:46:33 (bundle from f6e605ef, built 13:41); 16:51 rebuild → identical md5; `git show f6e605ef:dist/index.mjs \| md5sum` matches |
| 5 tmux TUI probe | ✅ | fresh pi cold-started on current bundle; `/team-help` rendered; `/team-dashboard` + `\x1bOA` (app-cursor up) + `j` moved the run-list selection `›` between runs (capture-pane diff 10 lines) |
| 6 pty probe | ✅ | `scripts/pty_probe.py --keys 'j,j,k,\x1bA,q,q' --startup-sleep 4` exit 0; frames rendered; double-q quit clean; no zombie (script reaps) |
| 7 smoke team run | ✅ | runId `team_20260816095535_770b17f77b28c3e8` (fast-fix, sync), 3/3 tasks, consistency=1; verifier gate 33.9s (<300s hard, <60s soft); worker exits all 0 |
| 8 final md5 sync | ✅ | disk unchanged `f3b0e7e5…` through the whole battery |
| 9a read-only battery | ✅ | 10/10 — list / recommend / health / doctor(zombies: 0) / status(compact) / events(full lifecycle incl. worker.spawned→exit) / summary(cost by role) / get(workflow implementation) / explain(markdown table) / worktrees(none). All structured; zero `Unknown type` / `Validation failed` |
| 9b spawn paths | ✅ | 5/5 — sync (=T7) · async `team_20260816100945_27a748c72d83a6cf` 3/3 echoes · chain 2/2 success (workflow omitted per issue #44; steps `team_20260816100945_e27f57be6cf087f9` + `team_20260816101348_68848a98a90550fe`) · `Agent` direct → `DIRECT-AGENT-OK` · `crew_agent` bg + `get_subagent_result` → `CREW-BG-OK` |
| 9c lifecycle | ✅ | status(details=true) full dump ✅ · cache ✅ · checkpoint ✅ · resume ✅ · retry-guard ✅ · invalidate ✅ · **LIVE mid-run battery (2nd session in tmux, 2026-08-16 15:24)**: run `team_20260816152402_7c04d41b8c3a8707` (fast-fix, sleep-300 tasks) — status(details=true) LIVE (01_explore running, real child-process, live events) ✅ · steer(runId+taskId) queued into live task ✅ (`steer requires runId+taskId` — schema-doc gap F2 family) · cancel → foreign-run ownership guard (F8: cross-session cancel blocked w/o force, then force:true cancelled) ✅ · plan-approval waiting flow: run `team_20260816152601_c2f4e8fcfae69f7d` (requirePlanApproval:true) → blocked + planApproval.pending ✅ · respond handler exercised (param validation, lookup, scan → structured "No waiting tasks" + actionable hint) ✅ · api(operation=follow-up-agent) → schema rejection of `operation` field (structured, no crash; the hint references the dashboard api surface, not team-tool — F9) |
| 9d destructive | ✅ | prune keep=40 → "Kept: 17, Removed: 0" + audit log ✅ · cleanup dryRun preview (.crew preserved by design) ✅ · forget throwaway 9c run (state+artifacts removed) ✅ |
| 9e admin | ✅ | workflow CRUD round-trip: create `rt9e-probe` (user tier, ~/.pi/agent/workflows) → get (2 steps) → workflow-list (13, probe present) → validate (11 agents/6 teams/13 workflows; 1 pre-existing WARNING dwf-smoke "no steps" — dynamic workflow, benign) → delete with `.bak` backup ✅ |
| 9f background | ✅ | auto-summarize status (disabled by default) ✅ · anchor (no-anchor message) ✅ · auto_boomerang (toggles auto-summarize in-session; config file verified untouched) ✅ · api(runId) full JSON dump ✅ · schedule: Feb-31 cron correctly REJECTED at registration; valid cron registered `8ad4b983…` → scheduled list (1 job, next 2027-01-01) → subAction=remove ✅ · goal-loop runKind dispatch `team_20260816102813_340f606d9a30a2a1` 4/4 consistency=1 (verifier scrubbed 18 PI_CREW_* env vars per knowledge gotcha, gates 22.2s) ✅ |

Legend: ✅ pass with evidence · ❌ fail (root cause below) · ⏭️ skipped (justify why)
*9c core actions all exercised; the two mid-run actions are unreachable from a single agent turn (F1) — not a product regression.

## Findings (bugs / quirks / non-blocking notes)

- **F1 (tool semantics, pre-existing)**: `team action='run' async=true` blocks the calling turn until the run completes (waitForRun). Worked around for the live battery by spawning a SECOND pi session in tmux (`/team-run --async`) — the run is then live while session 1 issues status/steer/cancel against it. No product change needed; documented pattern.
- **F8 (ownership, by design)**: `team action='cancel'` on a run owned by another session → "Run belongs to another session. Use force: true to override." Cross-session cancel protection works as intended (verified live); force:true cancels.
- **F9 (schema surface)**: `respond`'s error hint suggests `api operation=follow-up-agent`, but the team-tool `api` action rejects an `operation` field at schema level. The hint references the dashboard api surface, not the team tool. Minor doc mismatch.
- **F2 (minor doc)**: `checkpoint` requires BOTH runId AND taskId; the error message says so ("Checkpoint requires runId and taskId.") but the tool schema description does not — first probe returned the guard error. Zero impact.
- **F3 (expected lock behavior)**: calling `retry` in the same parallel batch as `resume` (same run) → "Run 'run.lock' is locked by another operation." Solo retry works and returns the correct guard ("retry only applies to failed/cancelled runs"). Parallel lock contention behaves as designed.
- **F4 (side effect, in-memory only)**: `auto_boomerang` enables auto-summarize for the session ("Auto-summarize enabled."). Verified `~/.pi/agent/pi-crew.json` has no autoSummarize key — no persistent mutation.
- **F5 (pre-existing state, not from this battery)**: health shows running=10 (stale in-memory snapshots from earlier sessions), 4 corrupted old runs, 4 stuck tasks, 21 zombie /tmp workspaces — leftovers from this week's test storms. `doctor zombies`: 0 orphan processes. Cleanup candidate for post-merge housekeeping (`prune` with lower keep + `/tmp` sweep).
- **F6 (multi-session)**: two `pi` processes live (08:15 leftover + this 16:46 session) — the sibling holds an older bundle; harmless here (no cross-session testing).
- **F7 (validation surfaced)**: `validate` flags `workflow:dwf-smoke: Workflow has no steps.` — dynamic (.dwf.ts) workflow; expected shape, benign warning.

## What was NOT run + why

- **task-level `respond` with a truly-waiting task** — no standard team-run path creates task status "waiting" (it is an interactive/live-agent extension state). The plan-approval flow reaches manifest `blocked` + `planApproval.pending` (not task waiting) — respond correctly reports "No waiting tasks" there. Full task-waiting→respond requires the live-agent/interactive machinery, out of scope for this battery.
- **`prune` with actual removals** — keep=40 found nothing eligible to remove (17 finished runs in this cwd, all within keep window). Handler proven end-to-end earlier today (28 runs pruned 2026-08-08 per knowledge.md) and the audit-log write path exercised now.
- **real `npm test` inside Tier 7** — forbidden by the skill's Tier 7 anti-pattern (worker timeout); verifier used the pinned fast command.

## Restart needed?

- [x] No — session already on the new bundle (T4/T8: loaded md5 = disk md5 = committed md5)

## Verdict

All 9 tiers pass with per-row evidence. 9c now includes the LIVE mid-run battery (steer queued live, live cancel via force, plan-approval waiting flow, respond handler exercised) run via a second tmux pi session; the only unexercised leaf is task-level respond against a literal task-waiting state, which no standard run path produces (interactive-agent surface). No failures, no test updates required (nothing outdated — battery ran against current code). v0.10.0 bundle (f6e605ef) is live-proven end-to-end: **safe to merge PR #46**.
