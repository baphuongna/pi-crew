# Real-Test Report — T3 / v0.10.1 (WP-5 governed nesting, ADR-5)

**Date:** 2026-08-19 · **Train:** T3 · **Bundle:** `157850723338` (rebuilt at PR #49 head `52ee6c69`) · **Live workspace:** `/tmp/b2-ws` (git project)

## Standard tiers

Partially deferred to the T5 full 9-tier re-run (B5) per the single-release battery strategy; T3-scoped probes below.

## B3 extra cases (ADR-5 appendix, security-weighted)

### (a) Flag-off + config containment — ✅ PASS live (production `loadConfig` in a real workspace)
1. **No config anywhere** → effective `nesting = {enabled:false, maxDepth:2}` — the `applyNestingDefaults` wiring holds the fail-closed default (P3-12 fix verified live).
2. **Project `.crew/config.json` `nesting.maxSlots=4`** → merges (`enabled` stays false) — non-sensitive knobs work from the project tier.
3. **Project tries `nesting.enabled=true`** → **DROPPED**: effective stays `{enabled:false, maxDepth:2}` + explicit warning `project-level sensitive config 'nesting.enabled' is ignored; set it in user config to trust it explicitly` — the S1-adjacent privilege-escalation path (cloned repo enabling delegation) is closed at the config layer, not just in the broker.

### (b) Happy path E2E — ✅ unit-E2E (`delegate-broker.test.ts` 5/5: immediate ref → fenced mailbox inbox delivery → reserve 500 → actual 300 roll-up on disk → slot release → second delegate admitted); **NOT live** — a real grandchild spawn requires `nesting.enabled=true` in USER config + a live executor worker with broker creds; the production spawner would mint a real model-backed pi process. Re-run live at B5 if the default flips.

### (c) Env containment — ✅ unit (`depth-override.test.ts`: issuer gate matrix, no-broker-keys shape; child-pi-env-spread gate suite green). Live depth-2 spawn deferred with (b).

### (d) Depth gate — ✅ unit (spawn-policy 24: depth-3 blocked at default; raised maxDepth → depth-3 admitted; pre-v2 absent depth = 1). **Depth-advance-per-hop proven structurally by the cold-verifier** (subId-scoped token + shadow record → `conn.taskId = subId` → admission reads the shadow's depth — the unbounded-chain fix).

### (e) Anti-deadlock — ✅ unit (`nested-slots-deadlock.test.ts`: sem-4 box with ALL global slots held → 2 delegates complete (rev-1 shape would deadlock); exhaustion rejects immediately 1/1; idempotent acquire/release).

### (f) Broker auth matrix — ✅ unit (`delegate-broker`: legacy bare-runId token forbidden with migrate hint; task-scoped compound required).

### (g) Budget — ✅ unit-E2E (reserve→roll-up→refund, clamp [0,reserve]; happy-path asserts `tokensSpent === 300` from disk). **Production reachability note**: no `task.allocation` producer ships yet (ADR follow-up) — budget admission is fail-closed (any `budgetTokens` on a real task → `budget-insufficient`), which is the SAFE direction.

### (h) Timeout — ✅ unit (`delegate-tool`: no mailbox entry → exact `[delegate timed out]`, server-normalized deadline; spawner AbortController + soft-cancel path exercised in delegate-spawn tests; broker `delegate.timed_out` event).

### (i) Model validation — ✅ unit (catalog member admitted / non-member denied; catalog-loader failure → EMPTY catalog → deny-all fail-closed — verifier item 5).

### (j) Resilience — ⛔ NOT live (socket-kill mid-grandchild needs (b)'s live setup). Durable-delivery design (mailbox write is file-locked, fire-and-forget independent of the connection; tool self-polls the file) means the result survives a broker restart by construction; pinned at B5.

### (k) Role/extension containment — ✅ unit (role threading verified end-to-end by the cold-verifier chain cite; allowlist across THREE rewritten legacy suites — `v0-7-9-interop-granularity`, `child-pi-timeout`, `project-agent-extensions-rce` 10/10 — user/builtin/project extensions all stripped at depth>0, trust gate superseded).

### (l) Workspace — ✅ unit (gate 9 matrix: write-capable + overlap + no-serialization → reject with hint; read-only never conflicts; serialization admits). Semantics per ADR-5 **erratum §9** (reject-on-overlap — grandchildren bypass the dispatch queue).

### (m) Security sign-off — ✅ **three recorded verdicts**: security-reviewer (10 findings → all applied), reviewer (14 findings → residuals applied), cold-verifier (**APPROVE**, 9/9 closed from source + 12/12 required suites; N1/N2 applied after).

### (n) Trust gate — ✅ unit (policy matrix) + broker integration (`nestingTrustedEscalation` threads; untrusted → `trust-denied` with manual-only message; delegation-broker tests opt in explicitly).

## Honest NOT-captured
- (b)/(c)/(j) **live** grandchild spawn/socket-kill (needs user-config flag flip + real worker + real model spend) — structural/unit/E2E proofs recorded; single live re-run folded into **B5** if/when the default flips.
- The delegate tool inside a REAL worker TUI session (mock workers used in prior batteries don't register worker-side tools) — tool lifecycle pinned by unit tests (7/7).

## Open findings from this battery
- None new. (All review-loop findings applied; N3 crash-window slot/shadow leak remains ADR-acknowledged follow-up with the startup-reconcile design note.)

## Verdict
**T3 clear to merge PR #49 once CI is green.** The fail-closed posture is verified at every layer live-probeable today: config default, project-tier escalation drop, and (behind the flag) the full admission/auth/containment matrix per the three review verdicts.
