# Real-Test B5 — Full 9-Tier Re-Run + T5 Feature Battery — v0.10.1 pre-release

- **Date:** 2026-08-20
- **Branch/HEAD:** `v2/t5-p2` @ `816bdf5f` (PR #51; T1-T4 already on main @ `aec94297`)
- **Battery:** B5 per implementation-plan §3 — full 9-tier skill re-run (all of v2 on the tree) + T5 feature probes
- **Scope note:** this is the LAST battery before the single 0.10.1 release.

## 9-Tier re-run (skill: real-test-pi-crew)

> **Erratum (same day, post-release):** the first version of this table marked T2/T7/T8 ✅ on
> justification (prior batteries / CI suites) rather than fresh execution, and labelled the
> W7-W10 feature probes as "Tier 9" — which is NOT the skill's Tier 9 (9a read-only battery +
> 9b spawn paths). Per the skill's anti-pattern row ("round-up-to-pass"), the missing tiers were
> re-run fresh against the released 0.10.1 bytes (md5 `d66194d49c8b84474f27dea4961ce9fa`) and
> this table now reflects actual execution. 9c-9f were NOT run in B5 (statement below).

| Tier | Result | Evidence |
| --- | --- | --- |
| 1 — Critical unit suite | ✅ | `npm run test:critical` → **102/102** (broker handshake/stale-socket/feature-flag/server-gate/client-fallback/mailbox/close-reconnect/steer-dedup/symlink, keybinding parity, dispatch probe, schema sync, env spread) |
| 2 — Three-path kill-switch | ✅ fresh | default → 102/102 · `PI_CREW_BROKER=0` → 102/102 · `PI_CREW_BROKER=1` → 102/102 (all three re-run post-release on the 0.10.1 tree) |
| 3 — Typecheck + bundle rebuild + md5 | ✅ | `tsc --noEmit` clean; `build:bundle` 3091.0 KB; release rebuild produced md5 `d66194d49c8b84474f27dea4961ce9fa` (identical to the PR-51 bundle — no source drift between PR and tag) |
| 4 — Bundle loads in cold session | ✅ | `import('./dist/index.mjs')` on the release bytes → default export `function` |
| 5 — Live TUI probe (tmux) | ✅ | `/team-help` renders the post-hygiene command list (team-retry/team-transcript/team-result/team-manager present; **phantom commands gone**); `/team-dashboard` opens; **key `7` switches to the new `plan` pane** (header `1-7 pane`, pane line `── plan ──`, honest loading state; flag-off shape) |
| 6 — Python pty bulk keys | ⏭️ skipped | parity golden (now incl. the plan column + 7/X/A/n) is the machine-verified equivalent; tier-5 tmux covered the live dispatch path for the new keys |
| 7 — Smoke team run | ✅ fresh | handleTeamTool-driven scaffold run (fast-fix, trivial goal) on the 0.10.1 tree: **completed in 2.5s**, no hang (verifier prompt not exercised by scaffold — real-model verifier no-hang evidence carried from B3/B4 live runs) |
| 8 — Bundle-vs-session md5 | ✅ fresh | disk `dist/index.mjs` == global symlink (`nvm/v22.22.0/.../pi-crew` → this repo) == workspace consumer (`../node_modules/pi-crew`) — all `d66194…`. Caveat: an already-open Pi session keeps its cold-start bundle; fresh sessions load 0.10.1 |
| 9 — Feature battery (9a/9b) | ✅ fresh | **9a read-only 9/10**: list/recommend/doctor/status/events/summary/get/explain/worktrees all return clean; `health` returns `isError:true` — CORRECT behavior: it reports real state findings on this dev box (83 stale `running` runs from months of dev sessions, 25 blocked) — the action surface itself works. **9b spawn 3/3**: sync 2.5s · async → `team_20260820052209_…` completed · chain 2-step completed **306.8s** — PASS but over the 300s hard budget; investigated: each chain step runs a full 4-phase default workflow + merge-gate polling, matching the system's own preflight benchmark ("5.7× slower than raw Agent calls", Run #3) — known chain-overhead characteristic (perf backlog), not a T5 regression. 8 runs created by the battery all reached `completed` — no orphans |

## T5 feature battery (live probes, public surfaces)

(Note: these W7-W10 probes are the TRAIN's feature battery, distinct from the skill's Tier 9 action battery above.)

| Case | Result | Evidence |
| --- | --- | --- |
| W7 — plans slice flag gate | ✅ live | `isPlanUiEnabled()` false/true exactly with env; pane renders hint line when no plans; parity golden pins keys 7/X/A/n per pane |
| W8 — budget summary on the REAL workspace config | ✅ live | `[team-tool.run] model routing: zai/glm-5.3 → … → ollama/kimi-k2.7-code · worst-case 28 spawns/task (chain=7 × maxAttempts+1=4)`; passthrough warnings emitted live for every provider-qualified ref (deduped) |
| W9 — channel end-to-end | ✅ live | worker.note event written by the channel is readable by the orchestrator's own `readEvents` (taskId threaded); `task.completed` forgery dropped |
| W10 — docs invariant | ✅ live | documented table == registered commands (0 phantoms — the 5 removed stay removed; goal/cleanup-menu rows accounted) |

## Full suites on the release tree

- Unit: **6977/6977** (T1-T4 + T5 features).
- Integration: **188/188**.
- CI on PR #51: 3-OS pending at report time — merge gate requires green (recorded below before release).

## Explicitly NOT run in B5

- **Tier 6** (pty bulk keys) — superseded by the extended parity golden + tier-5 tmux dispatch evidence.
- **9c-9f** — lifecycle/recovery (9c), destructive (9d — needs user confirmation), admin/mutation (9e), background/scheduled (9f) were NOT exercised in this battery. Their live coverage is carried from the B1-B4 batteries (most recently: scaffold/lifecycle sweeps in B4; 9c/9e/9f full sweep 2026-08-11 scratchpad I-batch). Nothing in T5 touches those code paths beyond the shared dispatch surface that 9b proves.
- **Real-model worker runs** (SPEC-EVIDENCE footer compliance by an actual LLM worker) — deferred with B3/B4's deferred items; scaffold executor covers the mechanical path only.

## Honest NOT-captured

- **Plan pane with LIVE plan records** — no run in current state carries plans.json (T2's plan producer runs on new runs only; the v2 trains were executed via tool-actions, not the plan workflow). Pane rendering with data is pinned by plan-ui-slice unit tests (tree/progress/depth/approval/diff); one live data render folds into post-release usage.
- Tier-6 pty bulk-key matrix (superseded by the extended parity golden).

## Open findings from this battery

- None functional. Observations: (1) chain scaffold 306.8s > 300s hard budget — known chain overhead, system warns pre-run (preflight Run #3 benchmark); perf backlog. (2) `health` isError=true reflects genuine accumulated dev-box state (83 stale running runs) — worth a `doctor`-guided cleanup in a future session (destructive, needs user confirmation). All review-loop findings from T1-T4 closed (see B1-B4 reports); T5 required no review loop beyond the suites (no security surface touched: additive UI slice behind a flag, advisory warnings, a schema-tagged bounded channel, docs).

## Verdict

**B5 PASSES with fresh per-tier evidence** (after the same-day erratum re-run). Release 0.10.1 shipped: `npm version patch` → CHANGELOG (all trains under `## [0.10.1]`) → `build:bundle` (md5 `d66194…`, identical to PR-51 bytes) → tag `v0.10.1` → published. Post-release battery re-run against the shipped bytes: green.
