# Real-Test Report — T1 / v0.10.1 (WP-1 identity, WP-2 ask/park, WP-3 approval UI)

**Date**: 2026-08-18 · **Bundle**: `dc2dedda4659…` (built from `1243c84f`) · **Branch**: `v2/t1` · **Battery**: B1 (plan §T1 exit) on top of the standard 9 tiers
**Workspace**: throwaway `/tmp/b1-ws` (git-initialized, `.crew/config.json` with `broker.waitMethodsEnabled:true`) · tmux sessions 190×50

## Standard tiers

| Tier | Status | Evidence |
|---|---|---|
| 1 test:critical | ✅ | 102/102 pass (env-scrubbed; one intermediate run had 2 broker files flake-hang 30s under load — isolated re-run 9/9, suite re-run 102/102, flake not reproducible) |
| 2 3-path kill-switch | ✅ | default + `PI_CREW_BROKER=0` + `=1` → 102/102 ×3, exit 0 |
| 3 typecheck + bundle | ✅ | `tsc --noEmit` 0; bundle rebuilt repeatedly; md5 `dc2dedda…` |
| 4 bundle sync | ✅ | sessions A–H cold-started on `dc2dedda` (config fixes verified live in-bundle) |
| 5 tmux TUI probe | ✅ | `/team-help` renders; `/team-dashboard` + pane `2` + banner `⚠ plan approval pending — A approve / n deny` visible live |
| 6 pty probe | ⊘ not run (tmux sufficient; no src/ui keybinding-dispatch change beyond map entries already parity-tested) |
| 7 smoke team run | ✅ | `team_20260818043043_016c831731ee7240` fast-fix sync 3/3, `BANANA` in explore+execute outputs, consistency=1 |
| 8 final md5 | ✅ | `dc2dedda…` stable through battery |
| 9a read-only | ✅ | 10/10 (list/health/doctor/status/events/summary/get/explain/worktrees/recommend) — all structured, zero Unknown type / Validation failed |
| 9b spawn | ✅ | sync (=T7) via `pi -p`; async `team_20260818104034_f183d4d8f6c90272` completed |

## B1 extra cases

### (a) Live ask→respond E2E, 2-session tmux — ✅ PASS (after 3 config-bug fixes)
Run `team_20260818104944_444811be7f60f05e` (fast-fix sync, goal forces ask, timeoutSec 600):
- `01_explore` parked via **raw broker protocol** (worker hand-rolled hello+wait.request through `PI_CREW_BROKER_SOCKET` — proves the wire contract); session H `respond force:true` → *"answered via mailbox (worker alive): 01_explore"* → explorer final output `ANSWER:ORANGE-42`.
- `02_execute` parked via the **real `ask` tool** (toolName:ask in transcript); second `respond force:true` delivered → fenced `<dependency-context>` answer → `ANSWER:ORANGE-42`.
- Events: 2× `ask.requested`, 4× `ask.answered` (2 per question: `delivery:"mailbox"` from the respond discriminator + terminal `wait.resolve` — both by design), run completed 3/3, `waitState` null at end, no double dispatch.
- Earlier probes isolated **three real config-layer bugs** (all fixed in `4c27b6aa` + `1243c84f`, regression tests added):
  1. **F1** `parseBrokerConfig` hand-rolled whitelist dropped `waitMethodsEnabled` (schema/defaults/ctor had it; parser silently stripped every config file).
  2. **F2** `mergeConfig` had no `broker` deep-merge section — any user-side broker object replaced the project block wholesale.
  3. **F3** lifecycle ctor read `loadConfig()` **without cwd** — workspace broker config never reached the production broker.
  4. **F4 (agents)** workers' `--tools` allowlist (agents/*.md frontmatter) hard-filtered the ask tool — the exact scratchpad f753be30 anti-pattern; `ask` added to all 11 builtin agents.

### (b) crash-recovery ask restore (kill -9 mid-question) — ❌ NOT RUN
Deferred: needs a dedicated parked-run + SIGKILL + restart cycle. Unit-level coverage exists (stale-reconciler-waiting.test.ts, respond-discriminator dead-path).

### (c) steer_subagent live on one-shot — ✅ PASS (after F7 root-cause fix)
Live chain proven post-fix (`ce5f45fd`, session R, run `team_20260818161247_6d17b756dc2b2539`):
spawn → steer append (74B steering file) → survives the model-fallback respawn window (previous wipe observed at +48s) → worker polls → verdict **REDIRECTED**.
Root cause found by forensic probes (0-byte steering files exist even for never-steered runs → spawn itself truncates): the WP-1 per-incarnation truncate (`child-executor.ts:596`) wiped ANY steer racing the dispatch→onSpawn gap — including across the fallback attempt loop (steer 15:59:11, wipe 15:59:59, NO-STEER verdict). Fix: one `taskDispatchStartedAtMs` before the attempt loop; truncates skip files written at/after it (stale residue still wiped). Also hardened: steer write-failure now errors loudly instead of reporting success.
Two earlier probe failures were NOT bugs: terminal-refusal on completed tasks (hardening working) and probe design (workers skipping the sleep).
Two `pi -p` probes: subagent ran, wrote `NO-STEER` — the steering note did not reach the worker within its window. Root cause not yet diagnosed (timing vs steering-file wiring for direct `crew_agent` spawns). Unit test `subagent-steer-live.test.ts` passes; live one-shot path unproven. **Open for next round.**

### (d) approval A/n keys live — ✅ PASS
Scaffold runs with `requirePlanApproval` (`team_…_fd650a63809d59c2`, `team_…_3ccd28c1e4bf9d15`):
- Dashboard row `⏸ … blocked`; pane `2` → banner live.
- `A` → `planApproval.status: approved` (manifest flip verified on disk).
- `n` → medium-danger confirm overlay (`Y=cancel plan and kill tasks`) → `Y` → run `cancelled`, `approval: cancelled`, banner gone.

### (e) legacy-token wait.* rejection — ✅ (unit-proven)
`wait-request-broker.test.ts`: legacy bare-runId token rejected with migrate-hint, nothing parked; cross-task `to` rejected. Live probe not repeated (same code path as (a) socket).

## Honest NOT-captured
- **Widget `⚠ plan:<id>` badge + powerbar `plan:pending` live screenshots**: NOT captured — the pending runs' rows scrolled behind toasts/history in every capture; unit + code-path coverage exists (ui-approval-surfaces 15/15). Next battery should capture within the visibility window.
- **6 pty probe** (see tier table).
- Sync-run UX: while a **sync** run parks on ask, the spawning session's input queue is blocked — respond must come from a second session **with `force:true`** (ownership guard). Documented behavior, not a defect; async runs are the natural home for ask — see F6.

## Open findings (from this battery)
| ID | Severity | Finding | Status |
|---|---|---|---|
| F1 | P1 | parseBrokerConfig dropped waitMethodsEnabled | **FIXED** `4c27b6aa` + test |
| F2 | P2 | mergeConfig missing broker deep-merge | **FIXED** `4c27b6aa` + test |
| F3 | P2 | lifecycle ctor loadConfig() without cwd | **FIXED** `1243c84f` + test |
| F4 | P2 | agents frontmatter `--tools` filtered ask tool | **FIXED** `4c27b6aa` (11 agents) |
| F5 | P2 | **async (detached) runs get NO broker creds** — ask unavailable in background runs (graceful notice, no hang); needs token-mint path or env-forward design | **OPEN** — needs decision (T2 ADR-0 amendment candidate) |
| F6 | P3 | sync-run ask parks block the session input; cross-session respond requires force:true | documented UX limitation |
| F7 | P2 | steer lost to per-incarnation truncate racing dispatch→onSpawn (incl. fallback respawn window); silent success on write failure | **FIXED** `ce5f45fd` — live REDIRECTED proof |
| F8 | P3 | `ask.answered` event also fires for timeout terminal-resolves (name suggests a real answer; state transitions correct) | cosmetic; note for T2 event-taxonomy pass |

## Verdict
T1 core surfaces are live-proven: **(a) ask/park/respond E2E ✅, (d) approval keys ✅, standard tiers ✅ (7 run + 9a/9b)**. The battery caught 4 real bugs (F1–F4) that every unit/scoped gate missed — all fixed with regression tests. F7 CLOSED (live REDIRECTED proof). Remaining before T1 can be called done: case (b) crash-recovery, F5 decision. **Do not merge PR #47 until those close.**
