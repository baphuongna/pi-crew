# US-022 — Export run report as markdown

## Status

verify-close — ALREADY IMPLEMENTED (`exportRunBundle` writes
`export/run-export.md`). Residual scope below.

## Lane / Priority

tiny · P3

## Source

- `docs/stories/backlog.md` — DX epic, US-022 (planned).

## Current state (verified 2026-09-22)

`src/extension/run-export.ts:39-96`:

- `exportRunBundle(manifest, tasks)` writes TWO artifacts:
  `export/run-export.json` (full bundle w/ sha256) and
  `export/run-export.md` (human report: runId, exportedAt, status, team,
  then tasks).
- Exposed through the export action; exercised by the export/import round
  trip and `run-bundle-schema.ts` validation.

So the backlog item "export run report as markdown" was satisfied by the
export work (see also the worker-events `time` fix in commit `8772eb37`,
which made the whole bundle importable — it previously rejected bundles
because surface-run events lacked `time`).

## Residual scope (what a "verify-close" pass should check)

1. **Markdown completeness** — does the report include: cost/token totals,
   model routing, durations, failed tasks with reasons, artifact list?
   Current body is minimal (header + tasks). Extend only if clearly useful;
   do not gold-plate.
2. **Determinism** — `exportedAt: new Date()` is in JSON (fine, metadata)
   but the sha256 covers it, so two exports of the same run differ by that
   field. Decide: pin via injectable `now` (see `createRunManifest`'s new
   `now` param pattern) so exports are byte-stable → enables diffing and
   US-021 reuse.
3. **Docs** — is the export action documented in README/SKILL?

## Acceptance Criteria

1. A decision recorded on `exportedAt` determinism (pin or documented
   accept).
2. If the markdown gains sections, they are deterministic and covered by
   the export unit test.
3. If nothing changes: close US-022 with a TEST_MATRIX row +
   backlog-status update.

## Validation

| Layer | Test | Expected |
|-------|------|----------|
| Unit | `test/unit/extension/run-export-determinism.test.ts` (new, if pinning) | 2 exports byte-equal except metadata rule |
| Unit | existing export/import + bundle-schema suites | 0 fail |
| Gates | `npm run test:critical` · `npm run typecheck` · biome | green |

## Notes

- This is a close-out spec: expected outcome is either a small determinism
  patch or an explicit "already done" status flip — not a feature build.